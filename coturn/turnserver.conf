# Static baseline settings. Deployment-specific values (realm, server-name,
# static-auth-secret, relay port range) are passed as CLI args in
# docker-compose.yml, sourced from .env — see the `coturn` service.
listening-port=3478
fingerprint
lt-cred-mech
no-multicast-peers
no-cli
no-tlsv1
no-tlsv1_1
